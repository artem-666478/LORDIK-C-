#include <iostream>  
#include <math.h>  
using namespace std; 
 
/* 
int main() { 
    int d; 
 int a; 
 int b; 
 int c; 
    int S; 
 
    cout << "a"; 
    cin >> a; 
    cout << "b"; 
    cin >> b; 
    cout << " c"; 
    cin >> c; 
    d = a * b * c; 
    cout << d<<" /n"; 
    S = 4 * a * a; 
    cout << S; 
} 
*/ 
 
/* 
int main() { 
 int F; 
 int m1=1; 
 int m2=2; 
 int r=3; 
    int G = 6.67; 
 int a = 10 ^ -11; 
    cout << "a"; 
    cin >> m1; 
    cout << "b"; 
    cin >> m2; 
    cout << " c"; 
    cin >> r; 
 
 F = G * a * (m1 * m2) / r ^ 2; 
 cout << F; 
} 
*/ 
 
/* 
int main() { 
 
 int S; 
 int S1=20; 
 int S2 = 0 > 20; 
 S = S2 - S1; 
 cout << S; 
} 
 
*/ 
 
/* 
int main() { 
    int v; 
    int v1; 
    int a; 
    int t; 
    cout << "chislo1"; 
    cin >> v; 
    cout << "chislo2"; 
    cin >> v1; 
    cout << "chislo3"; 
    cin >> a; 
    t = (v - v1) / a; 
    cout << t; 
} 
 
int main() { 
 
    double v1, a1, v2, a2, s; 
    std::cout << "Введите начальную скорость первого тела: "; 
    std::cin >> v1; 
    std::cout << "Введите ускорение первого тела: "; 
    std::cin >> a1; 
    std::cout << "Введите начальную скорость второго тела: "; 
    std::cin >> v2; 
    std::cout << "Введите ускорение второго тела: "; 
    std::cin >> a2; 
    std::cout << "Введите начальное расстояние между телами: "; 
    std::cin >> s; 
 
    // Вычисление времени встречи 
    double a = (a1 + a2) / 2;     // Коэффициент при t^2 
    double b = v1 + v2;           // Коэффициент при t 
    double c = -s;                // Свободный член 
 
    double discriminant = b * b - 4 * a * c; 
 
    if (discriminant >= 0) { 
        double t = (-b + sqrt(discriminant)) / (2 * a); 
 
        std::cout << "Время до встречи: " << t << " секунд" << std::endl; 
    } 
    else { 
        std::cout << "Тела не встретятся." << std::endl; 
    } 
 
    return 0; 
} 
*/ 
 
/* 
int main() { 
    int z; 
    int x; 
    int y; 
    cout << "x"; 
    cin >> x; 
    cout << "y"; 
    cin >> y ; 
    if (x > y) { 
        z = x - y; 
    } 
    else { 
        z = y - x + 1; 
    } 
 
    cout << z; 
} 
*/ 
 
/* 
int main() { 
    int x; 
    int y; 
    int z; 
    cout << "1 chislo"; 
    cin >> x; 
    cout << "2 chislo"; 
    cin >> y; 
    cout << "3 chislo"; 
    cin >> z; 
    if (x < 3 && x > 1) { 
        cout << x; 
    } 
 
    if (y < 3 && y > 1) { 
        cout <<y; 
    } 
 
    if (z < 3 && z > 1) { 
        cout << z; 
    } 
} 
*/ 
 
/* 
int main() { 
    int x; 
    float y; 
    cin >> x; 
    //a 
    if (y = -x) { 
        cout << y; 
    } 
    if (y = -(x * x)) { 
        cout << "   " << y; 
    } 
    //b 
    if (y = 1 / pow(x, 2)) { 
        cout << "  " << y; 
    } 
    if (y = pow(x, 2)) { 
        cout << "  " << y; 
    } 
    if (y == 4) { 
        cout << "  " << y; 
    } 
    //в 
    if (y = 1 / pow(x, 2)) { 
        cout << "  " << y; 
    } 
 
} 
*/ 
 
/* 75 
void sum(int gg) { 
    int a = 1; 
    int b = 1; 
 
    while (true) 
    { 
        int y = 3 * a + 5 * b; 
        if (y == gg) { 
            cout << a << " " << b; 
            break; 
        } 
        if (y < gg) { 
            a++; 
            b++; 
        } 
        if (y > gg) { 
            a--; 
        } 
        if (a < 0) { 
            a = a + 5; 
 
            b = b + 3; 
        } 
    } 
    cout << endl; 
} 
 
int main() { 
    int size; 
    cin >> size; 
    sum(size); 
} 
*/ 
 
/* 
int factorial(int n) { 
    if (n > 0) 
        return (n * factorial(n - 1)); 
    else { 
        return 1;        
    } 
} 
 
int main() { 
    float x=1; 
    int y; 
    y = x -(pow(x,3) / factorial(3))+(pow(x,5)/factorial(5))-(pow(x, 7) / factorial(7))+(pow(x, 9) / factorial(9))-(pow(x, 11) / factorial(11))+(pow(x,13) / factorial(13)); 
     
    cout << y; 
} 
 
*/ 
 
int main() { 
    cout << "       ____________\n"; 
    cout << "       |          |\n"; 
    cout << "       |          |\n"; 
    cout << "       |          |\n"; 
    cout << "       |          |\n"; 
    cout << "       |          |\n"; 
    cout << "       |          |\n"; 
    cout << "      —————————————\n"; 
    cout << "      /            \\\n "; 
    cout << "    /              \\\n "; 
    cout << "   /                \\\n "; 
    cout << "  /                  \\\n "; 
    cout << " /                    \\\n "; 
    cout << "/                      \\\n "; 
    cout << "(                      )\n "; 
    cout << "(                      )\n "; 
    cout << "  \\                   /\n "; 
    cout << "   \\                 /\n "; 
    cout << "    \\               /\n "; 
    cout << "     \\             /\n "; 
    cout << "      \\           /\n "; 
    cout << "       \\         /\n "; 
    cout << "        \\       /\n "; 
    cout << "         \\     /\n "; 
    cout << "          \\   /\n "; 
    cout << "           \\ /\n "; 
    cout << "          ———— "; 
}

/* 80
#include <iostream>
#include <cmath>

using namespace std;

int main() {
  double x;
  cout << "Введите значение x: ";
  cin >> x;

  double result = x;
  for (int i = 3; i <= 13; i += 2) {
    if (i % 4 == 3) {
      result -= pow(x, i) / tgamma(i + 1);
    } else {
      result += pow(x, i) / tgamma(i + 1);
    }
  }

  cout << "Результат: " << result << endl;

  return 0;
}
*/

/* 148
#include <iostream>
#include <iomanip>

using namespace std;

int main() {
  cout << "цельсия" << setw(10) << "фаренгейт" << endl;
  cout << "-------" << setw(10) << "----------" << endl;

  for (int celsiy = 0; celsiy <= 100; celsiy++) {
    double fahrenheit = (9.0 / 5.0) * celsius + 32;
    cout << setw(4) << celsiy << setw(10) << fixed << setprecision(2) << fahrenheit << endl;
  }

  return 0;
}
*/
/* 201
#include <iostream>
using namespace std;

int main() {
    int n;
    cout << "Введите количество чисел: ";
    cin >> n;

    double sum = 0;
    double number;

    cout << "Введите " << n << " чисел: " << endl;
    for (int i = 0; i < n; i++) {
        cin >> number;
        sum += number; // Суммируем числа
    }

    cout << "Сумма введенных чисел: " << sum << endl;
    return 0;
}
*/
